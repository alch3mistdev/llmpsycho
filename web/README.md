# Profile Studio Web

React + Vite frontend for interactive profile creation/exploration and query-lab A/B analysis.

## Dev

```bash
npm install
npm run dev
```

By default the app calls `http://localhost:8000`.
Override with:

```bash
VITE_API_BASE=http://localhost:8000 npm run dev
```
